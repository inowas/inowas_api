FOS\UserBundle\Model\User:
    exclusion_policy: ALL
    properties:
        id:
            expose: true
            groups: ["details", "list"]
        username:
            expose: true
            groups: ["details", "list"]
        email:
            expose: true
            groups: ["details", "list"]
        enabled:
            expose: false
        locked:
            expose: false